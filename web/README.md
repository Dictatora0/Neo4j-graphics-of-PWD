# 松材线虫病知识图谱 Web 应用

交互式知识图谱可视化与探索平台

## 📋 功能特性

- 🎨 **交互式图谱可视化** - 基于 Cytoscape.js 的力导向图布局
- 🔍 **智能搜索与筛选** - 全文搜索、多维度筛选
- 📊 **数据统计面板** - 实时图谱统计与分析
- 🔗 **路径分析** - 节点间关系路径查询
- 💾 **数据导出** - 支持图片、JSON、CSV 导出
- 📱 **响应式设计** - 适配桌面端和移动端

## 🚀 快速开始

### 方式一：一键启动（推荐）

使用智能启动脚本，自动检测端口冲突并启动所有服务：

```bash
cd web
./start.sh
```

**功能特性：**

- ✅ 自动检测端口占用
- ✅ 智能释放冲突端口
- ✅ 检查依赖完整性
- ✅ 后台运行服务
- ✅ 实时状态显示

**其他脚本：**

```bash
./status.sh   # 查看服务状态
./stop.sh     # 停止所有服务
./restart.sh  # 重启所有服务
```

### 方式二：手动启动

#### 环境要求

- Node.js >= 18.x
- Python >= 3.10
- Neo4j >= 5.x

#### 后端启动

```bash
cd web/backend
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

访问 API 文档: http://localhost:8000/docs

#### 前端启动

```bash
cd web/frontend
npm install
npm run dev
```

访问应用: http://localhost:5173

### 方式三：Docker 部署（开发中）

```bash
cd web
docker-compose up -d
```

## 📁 项目结构

```
web/
├── backend/          # FastAPI 后端
├── frontend/         # React 前端
├── docker/           # Docker 配置
└── README.md         # 本文档
```

## 🛠️ 技术栈

### 前端

- React 18 + TypeScript
- Vite 5
- TailwindCSS + shadcn/ui
- Cytoscape.js (图谱可视化)
- React Query (数据管理)
- Zustand (状态管理)

### 后端

- FastAPI 0.109+
- Neo4j Python Driver 5.x
- Pydantic 2.x
- Uvicorn

## 📖 开发文档

详见 [WEB_DEVELOPMENT_PLAN.md](./WEB_DEVELOPMENT_PLAN.md)

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License
