# 核心依赖
PyMuPDF==1.24.1
pandas==2.1.4
numpy==1.26.2
tqdm==4.66.4
pyyaml==6.0.2

# NLP 依赖
spacy==3.7.4
jieba==0.42.1
scikit-learn==1.7.2
yake==0.4.8

# KeyBERT（可选）
keybert==0.8.5
sentence-transformers==2.3.1
huggingface-hub>=0.19.0

# 图数据库
neo4j==5.15.0

# OCR 支持（可选）
pytesseract==0.3.10
pdf2image==1.17.0
Pillow==10.4.0

# 表格提取（可选）
# camelot-py==0.11.0
# tabula-py==2.9.0

# PaddleOCR（可选，中文效果更好）
# paddlepaddle==2.5.2
# paddleocr==2.7.0

# 视觉模型（可选）
# transformers==4.35.2
# torch==2.1.1

# LLM 支持（可选）
# openai==1.6.1

# Ollama 支持（用于本地 LLM）
requests==2.32.3

# 嵌入模型（用于概念去重）
sentence-transformers==2.3.1

# PDF处理备选（可选）
# pdfplumber==0.10.3

# 智能文档解析
marker-pdf==1.10.1  # 用于Layout-Aware解析
python-multipart>=0.0.6  # 文件处理