# 知识图谱查询脚本使用指南

## 📋 简介

`query_kg.py` 是一个功能强大的知识图谱查询工具，提供交互式和自动化两种模式，用于展示和探索松材线虫病知识图谱的成果。

## 🚀 快速开始

### 1. 安装依赖

```bash
pip3 install --break-system-packages neo4j tabulate colorama
```

### 2. 运行脚本

#### 自动模式（推荐）

一键查看所有统计信息和核心数据：

```bash
python3 query_kg.py --auto
```

这将自动运行以下查询：

- 📊 数据库统计信息
- 📈 节点度数分布（Top 20）
- 🔍 各类型节点示例
- 🌐 核心节点群落分析
- 📋 知识三元组展示

#### 交互模式

进入交互式菜单界面：

```bash
python3 query_kg.py
```

## 🎯 功能列表

### 1. 数据库统计信息

- 总节点数和总关系数
- 平均度数
- 节点类型分布
- 关系类型分布

### 2. 节点度数分布

展示连接最多的核心节点（Top 20）

### 3. 按类型查看节点

- **Pathogen**: 病原体
- **Host**: 寄主植物
- **Vector**: 传播媒介
- **Disease**: 病害症状
- **Location**: 地理位置
- **Control**: 防控措施
- **Environment**: 环境因素

### 4. 搜索节点

根据关键词模糊搜索节点

### 5. 查看节点关系

查看指定节点的所有关系

### 6. 按类型查看关系

查看特定类型的关系（感染、引起、传播等）

### 7. 最短路径查询

查找两个节点之间的最短连接路径

### 8. 核心节点群落分析

分析连接紧密的核心节点

### 9. 知识三元组

查看知识图谱的三元组表示（主语-谓语-宾语）

## 📊 典型查询示例

### 查看整体统计

```bash
python3 query_kg.py --auto
```

### 搜索特定概念

```bash
python3 query_kg.py
# 选择 4
# 输入: 线虫
```

### 探索关系网络

```bash
python3 query_kg.py
# 选择 5
# 输入: 松材线虫
```

## 🔧 命令行参数

| 参数         | 说明             | 默认值                  |
| ------------ | ---------------- | ----------------------- |
| `--uri`      | Neo4j 连接地址   | `bolt://localhost:7687` |
| `--user`     | 数据库用户名     | `neo4j`                 |
| `--password` | 数据库密码       | `12345678`              |
| `--auto`     | 自动运行所有查询 | `False`                 |

## 📈 输出示例

```
总节点数: 156
总关系数: 254
平均度数: 3.26

节点类型分布:
节点类型      数量    占比
----------  ------  ------
Other         56    35.90%
Pathogen      24    15.38%
Host          21    13.46%
Disease       19    12.18%
...
```

## 💡 使用技巧

1. **快速了解**: 先用 `--auto` 查看整体
2. **深入探索**: 使用交互模式针对性查询
3. **导出报告**: 重定向输出到文件
   ```bash
   python3 query_kg.py --auto > report.txt
   ```

## 📚 相关文档

- **主 README**: `README.md`
- **Web 应用**: `web/README.md`
- **项目结构**: `web/PROJECT_STRUCTURE.md`
